#include <bits/stdc++.h>
#define ll long long
#define endl "\n"

using namespace std;

vector <ll> v[2001];
vector <ll>visited;
ll n, m;

void dfs(ll num)
{
    cout<<num<<' ';
    visited[num]=1;

    for(auto it=v[num].begin(); it!=v[num].end(); it++)
    {
        if(visited[*it]==0) dfs(*it);
    }
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>n>>m;
    visited.resize(n+1);
    while(m--)
    {
        ll u, k;
        cin>>u>>k;

        v[u].push_back(k);
        v[k].push_back(u);
    }


    dfs(1);

    return 0;
}
