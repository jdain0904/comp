#include <bits/stdc++.h>

#define ll long long
#define endl "\n"

using namespace std;

ll start, fin, dial;
vector <ll> visited(10000);
vector <ll> no;
queue <pair<ll, ll>> q;
ll n;



ll check(ll v)
{
    for(ll i=0; i<n; i++)
    {
        if(no[i]==v) return 1;
    }
    return 0;
}

void bfs()
{
    q.push({start, 0});
    visited[start]=1;

    while(!q.empty())
    {
        ll x=q.front().first;
        ll cnt=q.front().second;
        q.pop();

        if(check(x)) continue;
        if(x==fin)
        {
            cout<<cnt;
            return;
        }

        //print(x);

        ll num=1;
        ll save=x;

        for(ll i=0; i<4; i++)
        {
            x=(x+num)%10000;

            if(visited[x]==0)
            {
                visited[x]=1;
                q.push({x, cnt+1});
            }
            x=save;

            x=(x-num+10000)%10000;
            if(visited[x]==0)
            {
                visited[x]=1;
                q.push({x, cnt+1});
            }

            x=save;

            num*=10;
        }

    }
    cout<<-1;
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>start>>fin;
    cin>>n;

    for(ll i=0; i<n; i++)
    {
        ll temp;
        cin>>temp;
        no.push_back(temp);
     //   print(no[i]);
    }


    bfs();

    return 0;
}
